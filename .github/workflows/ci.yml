
name: Full CI
on:
  push:
    branches:
      - main
      - dev
  pull_request:

permissions: {}

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  python_snippets:
    name: Check Python snippets
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Install uv
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          python-version: "3.13"

      - name: Install Python deps
        run: uv sync --all-extras

      - name: Set up pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.19.0
          cache: pnpm

      - name: Install Node deps
        run: pnpm install --frozen-lockfile

      # Note: the notebook .mdx files are gitignored, so this gate only sees
      # hand-written pages. Notebook cells are syntax-checked by
      # tests/test_notebook_cells.py instead -- type-checking them would mean
      # fixing ~90 pre-existing Optional-access findings first.
      - name: Check Python snippets
        run: pnpm run test:python-snippets

      - name: Check API baselines
        run: uv run pytest tests/test_docs_refresh.py

  docs_build:
    name: Build docs
    runs-on: ubuntu-latest
    timeout-minutes: 15
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      - name: Set up pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: 24.19.0
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build site
        run: pnpm run build:ci

  doc_tests:
    name: Test and update notebooks
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: docs_build
    environment: docs-deploy
    permissions:
      contents: read # checkout
      pull-requests: read # list PR files via the gh api call below
    steps:
      - name: Checkout code
        uses: actions/checkout@3d3c42e5aac5ba805825da76410c181273ba90b1 # v7.0.1
        with:
          persist-credentials: false

      # Notebook execution is slow and needs provider API keys, so only run it
      # when the PR actually touches notebooks or the notebook test harness.
      # Pushes to main/dev always run (OVERWRITE_NB depends on it).
      - name: Detect notebook-relevant changes
        id: nbchanges
        env:
          GH_TOKEN: ${{ github.token }}
          REPO: ${{ github.repository }}
          PR_NUMBER: ${{ github.event.pull_request.number }}
        run: |
          set -euo pipefail
          if [ "$GITHUB_EVENT_NAME" != "pull_request" ]; then
            echo "relevant=true" >> "$GITHUB_OUTPUT"
            exit 0
          fi
          files=$(gh api "repos/$REPO/pulls/$PR_NUMBER/files" --paginate --jq '.[].filename')
          if echo "$files" | grep -Eq '\.ipynb$|^tests/(conftest|test_docs_nb)\.py$|^pyproject\.toml$|^uv\.lock$|^\.github/workflows/ci\.yml$|^scripts/convert-notebooks\.mjs$'; then
            echo "relevant=true" >> "$GITHUB_OUTPUT"
          else
            echo "relevant=false" >> "$GITHUB_OUTPUT"
          fi

      - name: Install uv
        if: steps.nbchanges.outputs.relevant == 'true'
        uses: astral-sh/setup-uv@20cfd1bf945f4377ade1205e4dbc17946fc9a30d # v10.0.1
        with:
          python-version: "3.13"

      - name: Install Python deps
        if: steps.nbchanges.outputs.relevant == 'true'
        run: uv sync --all-extras

      - name: Set up pnpm
        uses: pnpm/action-setup@0977fd99725f1db4007ccb2928dbb4e90d06cc86 # v6.0.10

      - name: Set up Node.js
        uses: actions/setup-node@820762786026740c76f36085b0efc47a31fe5020 # v7.0.0
        with:
          node-version: "24.19.0"
          cache: "pnpm"

      - name: Install Node deps
        run: pnpm install --frozen-lockfile

      - name: Build site
        run: pnpm run build:ci

      - name: Check MDX tables render
        run: pnpm run test:tables

      - name: Check redirects
        run: pnpm run test:redirects

      - name: Check SEO invariants
        run: pnpm run test:seo

      # The notebooks run against the Azure AI proxy behind AZURE_AI_API_BASE, whose
      # deployment names are not public model ids. The pages themselves name no model:
      # giskard-checks reads GISKARD_CHECKS_DEFAULT_MODEL on its own and otherwise uses
      # openai/gpt-4o-mini, which is what a reader gets. OPENAI_MODEL covers the raw
      # AsyncOpenAI calls in the example agents, which the SDK does not resolve itself.
      # Both come from the DOCS_NB_MODEL repository variable -- set it to a deployment
      # that exists on the proxy. Unset, it falls back to the deployment that works
      # today and warns, so a rename surfaces in the log instead of as six mystery
      # notebook failures.
      # Covers oss/checks and oss/scan; the full-scan notebooks are opt-in behind
      # RUN_SLOW_NOTEBOOKS=1 (see tests/test_docs_nb.py) because each costs minutes
      # and real money.
      - name: Run notebook tests
        if: steps.nbchanges.outputs.relevant == 'true'
        env:
          OPENAI_API_KEY: ${{ secrets.AZURE_AI_API_KEY }}
          OPENAI_BASE_URL: ${{ secrets.AZURE_AI_API_BASE }}
          DOCS_NB_MODEL: ${{ vars.DOCS_NB_MODEL || 'gpt-5.4-nano' }}
          OPENAI_MODEL: ${{ vars.DOCS_NB_MODEL || 'gpt-5.4-nano' }}
          GISKARD_CHECKS_DEFAULT_MODEL: openai/${{ vars.DOCS_NB_MODEL || 'gpt-5.4-nano' }}
          OVERWRITE_NB: ${{ github.ref == 'refs/heads/main' && '1' || '0' }}
        run: |
          if [ -z "${{ vars.DOCS_NB_MODEL }}" ]; then
            echo "::warning::DOCS_NB_MODEL is unset, falling back to gpt-5.4-nano. Set the repository variable so a deployment rename does not fail every notebook."
          fi
          uv run pytest tests/test_docs_nb.py -v --tb=short

      - name: Regenerate .mdx from updated notebooks (main only)
        if: github.ref == 'refs/heads/main'
        run: node scripts/convert-notebooks.mjs

      - name: Generate app token (main only)
        if: github.ref == 'refs/heads/main'
        id: app-token
        uses: actions/create-github-app-token@bcd2ba49218906704ab6c1aa796996da409d3eb1 # v3.2.0
        with:
          app-id: ${{ vars.RELEASE_APP_ID }}
          private-key: ${{ secrets.RELEASE_APP_PRIVATE_KEY }}
          permission-contents: write

      - name: Commit updated notebooks and docs (main only)
        if: github.ref == 'refs/heads/main'
        env:
          APP_TOKEN: ${{ steps.app-token.outputs.token }}
          REPO: ${{ github.repository }}
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add src/content/docs/oss/
          if ! git diff --staged --quiet; then
            git commit -m "chore: update notebook outputs and regenerate mdx [skip ci]"
            git remote set-url origin "https://x-access-token:${APP_TOKEN}@github.com/${REPO}.git"
            git push
          fi
